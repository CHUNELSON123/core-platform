info:
  name: Permission
  type: http
  seq: 10

http:
  method: POST
  url: http://localhost:3001 /permissions
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
