info:
  name: Refresh Token
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:3001/auth/refresh
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
