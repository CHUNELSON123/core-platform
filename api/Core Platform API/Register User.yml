info:
  name: Register User
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:3001/auth/register
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
