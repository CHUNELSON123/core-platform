info:
  name: Reset Password
  type: http
  seq: 10

http:
  method: POST
  url: http://localhost:3001/auth/reset-password
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
