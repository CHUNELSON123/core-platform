info:
  name: Role Permission
  type: http
  seq: 11

http:
  method: POST
  url: http://localhost:3001/role-permissions
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
