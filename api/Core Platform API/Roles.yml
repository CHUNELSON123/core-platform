info:
  name: Roles
  type: http
  seq: 11

http:
  method: POST
  url: http://localhost:3001/roles
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
