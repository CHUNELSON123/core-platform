info:
  name: User Role
  type: http
  seq: 12

http:
  method: POST
  url: http://localhost:3001/user-roles
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
